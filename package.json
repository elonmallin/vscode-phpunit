{
	"name": "phpunit",
	"displayName": "phpunit",
	"description": "Start phpunit tests from VSCode.",
	"version": "0.0.1",
	"publisher": "emallin",
    "icon": "images/phpunit.png",
	"engines": {
		"vscode": "^0.10.6"
	},
	"categories": [
		"Other"
	],
    "homepage": "https://github.com/elonmallin/vscode-phpunit",
    "bugs": "https://github.com/elonmallin/vscode-phpunit/issues",
    "license": "SEE LICENSE IN LICENSE.md",
    "repository": {
        "Github": "https://github.com/elonmallin/vscode-phpunit"
    },
    "keywords": [
        "phpunit",
        "test",
        "unittest",
        "php"
    ],
	"activationEvents": [
		"onCommand:phpunit.Test",
        "onCommand:phpunit.TestDirectory"
	],
	"main": "./out/src/extension",
	"contributes": {
		"commands": [{
			"command": "phpunit.Test",
			"title": "phpunit"
		},{
			"command": "phpunit.TestDirectory",
			"title": "phpunit directory"
		}],
        "configuration": {
            "type": "object",
            "title": "Phpunit Configuration",
            "properties": {
                "phpunit.execPath": {
                    "type": "string",
                    "default": "phpunit",
                    "description": "Path to phpunit executable."
                },
                "phpunit.args": {
                    "type": "array",
                    "default": [],
                    "description": "Any phpunit args (phpunit --help)"
                }
            }
        }
	},
	"scripts": {
		"vscode:prepublish": "node ./node_modules/vscode/bin/compile",
		"compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
	},
	"devDependencies": {
		"typescript": "^1.7.5",
		"vscode": "^0.11.0"
	}
}